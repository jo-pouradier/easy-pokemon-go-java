package com.example.pokemongeo_tp.threading;

import java.util.function.Function;

/**
 * Generic Listener pour les threads.
 * @param <T> type de données à envoyer
 * @param <R> type de données à recevoir
 */
public class Thread<T, R> extends java.lang.Thread {
//    private ThreadEventListener<R> listener;
//    private Function<T, R> callback;
//    private T data;
//
//    public Thread(ThreadEventListener<R> listener, Function<T, R> callback, T data) {
//        this.listener = listener;
//        this.callback = callback;
//        this.data = data;
//    }
//
//    public void run() {
//        while (true) {
//            R returnData = callback.apply(data);
//            listener.OnEventInThread(returnData);
//        }
//    }
}
